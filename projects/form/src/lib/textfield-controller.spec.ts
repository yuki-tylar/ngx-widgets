import { TextfieldController } from './textfield-controller';

describe('TextfieldController', () => {
  it('should create an instance', () => {
    expect(new TextfieldController()).toBeTruthy();
  });
});
