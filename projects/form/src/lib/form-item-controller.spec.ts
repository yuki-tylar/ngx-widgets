import { FormItemController } from './form-item-controller';

describe('FormItemController', () => {
  it('should create an instance', () => {
    expect(new FormItemController()).toBeTruthy();
  });
});
