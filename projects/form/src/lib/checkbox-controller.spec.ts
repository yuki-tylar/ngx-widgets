import { CheckboxController } from './checkbox-controller';

describe('CheckboxController', () => {
  it('should create an instance', () => {
    expect(new CheckboxController()).toBeTruthy();
  });
});
