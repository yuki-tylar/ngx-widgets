import { SelectMultiController } from './select-multi-controller';

describe('SelectMultiController', () => {
  it('should create an instance', () => {
    expect(new SelectMultiController()).toBeTruthy();
  });
});
